<% include ../partials/header %>
                        <ul class="nav navbar-nav">
                            <li class="active"><a id="graphs" href="/">Gráficos</a></li>
                            <li><a id="states" href="/list">Estados</a></li>
                            <li><a id="log" href="#">Histórico</a></li>
                        </ul>
                    </div>
                </div>
            </nav>

            <script>
                var statesInfo = JSON.parse(decodeURIComponent("<%= encodeURIComponent(JSON.stringify(data)) %>"));
                console.log(statesInfo);
            </script>
            
            <div class="row content">
                <div class="col-md-3 param-panel">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <label class="param-titles">Parâmetros</label>
                                <form id="param-rds">
                                    <input type="radio" name="param-radio" value="0" checked> Estado X População
                                    <br>
                                    <input type="radio" name="param-radio" value="1"> Estado X PIB
                                    <br>
                                    <input type="radio" name="param-radio" value="2"> Estado X PIB X População
                                </form>
                                <hr>
                            <label class="param-titles">Estados</label>
                                <% for (var i in data) { %>
                                    <div class="checkbox">
                                        <label><input type="checkbox"> <%= data[i].name %></label>
                                    </div>
                                <% } %>
                        </div>
                    </div>
                </div>
                <div class="col-md-1 arrow-div">
                    <div class="triangle-left"></div>
                </div>
                <div class="col-md-7 graph-panel">
                    <div class="panel panel-default">
                        <div class="panel-body graph-div">
                        </div>
                    </div>
                </div>
                <div class="col-md-1 arrow-div">
                    <div class="triangle-right"></div>
                </div>
            </div>

<% include ../partials/footer %>